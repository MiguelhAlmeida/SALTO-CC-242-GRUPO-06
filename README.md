# Clonar repositório: 

-git clone https://github.com/MiguelhAlmeida/sudokuceunsp.git

# Mudar para a sua branch: 

1- git fetch
2- git checkout nome_da_branch
3- git pull 

# Ao clonar o repositório pela primeira vez, seguir as instruções:

-python -m venv venv
-.\venv\Scripts\activate
-pip install -r requirements.txt

Ao ter o repositório já instalado na sua maquina, a partir da segunda vez ao abrir o projeto, rodar no terminar apenas uma vez a seguinte instrução: .\venv\Scripts\activate

# Subir alteração no git: 

-git add *
-git commit -m "escrever a alteração feita por voce"
-git push
